status: active
owner: DeerSpotter
agent: ChatGPT GPT-5.6 Sol

task: >-
  Generalize ipaSim's read-only chained-import inventory into a closure-wide,
  two-level-namespace static symbol audit that reports all unresolved bindings
  before DynamicLoader stops at the first runtime failure.

scope:
  - src/IpaSimulator/HostImportInventory.hpp
  - src/IpaSimulator/HostImportInventoryV2.hpp
  - src/IpaSimulator/IpaProbe.cpp
  - src/IpaSimulator/CMakeLists.txt
  - src/IpaSimulator/*SymbolAudit*
  - scripts/Test-Ipa.ps1

branch: compat-static-closure-symbol-audit
started_utc: 2026-08-31T00:25:00Z
updated_utc: 2026-08-31T00:25:00Z
base_sha: e1de920b9010f369c259e9b75c39ef689767bfd5
expected_pr: tbd

next_checkpoint: >-
  Reuse ipaSim's ARM64 chained-fixup parser and real dependency/ordinal rules to
  walk the full RuntimeRoot closure, compare each import against its expected
  provider (including the Windows host bridge), emit grouped unresolved bindings,
  and add a synthetic semantic smoke proving wrong-provider exports do not satisfy
  two-level imports.

notes: >-
  Generic public diagnostics only. The audit must remain read-only and must not
  suppress DynamicLoader failures, invent successful bindings, or include private
  application names, paths, RuntimeRoot contents, or target-specific assumptions.
