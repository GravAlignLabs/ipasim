status: active
owner: DeerSpotter
agent: ChatGPT GPT-5.6 Sol

task: >-
  Add one pinned, redistributable third-party iOS IPA as a public acceptance
  workload and run it through the published Windows ipaSim tester with both a
  no-secrets public loader check and a trusted full-runtime acceptance path.

scope:
  - .github/workflows/public-ipa-acceptance.yml
  - tests/public-ipas/aws-device-farm-sample-app.md
  - .github/agent-work/public-ipa-acceptance.yml

branch: test/public-ipa-acceptance
started_utc: 2026-09-03T02:32:00Z
updated_utc: 2026-09-03T02:46:00Z
base_sha: 32440b78283ad38d6b915bcabd78782d830746bf
expected_pr: draft #68

next_checkpoint: >-
  Preserve the no-secrets PR loader boundary, then add a trusted master/manual
  job that restores only the encrypted RuntimeRoot archive from Actions cache,
  extracts plaintext into runner.temp, and runs the pinned AWS IPA through the
  exact published Test-Ipa.cmd acceptance path.

notes: >-
  Test infrastructure only. The IPA is Apache-2.0 sample material from
  aws-samples and is referenced by immutable upstream commit/blob identity.
  Do not commit, artifact-upload, or plaintext-cache Apple RuntimeRoot material.
  A trusted non-PR CI job may reuse the existing encrypted RuntimeRoot archive
  cache and repository secrets; plaintext extraction must remain ephemeral in
  runner.temp and the privileged job must never run for pull_request events.
