status: active
owner: DeerSpotter
agent: ChatGPT GPT-5.6 Sol

task: >-
  Add a compiler-backed Win64 carrier ABI surface that consumes the merged AAPCS64
  ABI evidence, lowers equivalent fixed-width carrier signatures for x86_64 Windows,
  and emits deterministic guest-to-host transfer plans without pretending that a
  carrier adapter is itself a semantic implementation.

scope:
  - tools/compat_surface/win64_abi_surface.py
  - tools/compat_surface/test_win64_abi_surface.py
  - tools/compat_surface/README.md
  - .github/workflows/compat-surface.yml
  - .github/agent-work/win64-carrier-abi-surface.yml

branch: compat-win64-abi-surface
started_utc: 2026-08-31T14:14:00Z
base_sha: 70df613b36c1d6738f12fdf07808e91b3c1e3327
expected_pr: draft

next_checkpoint: >-
  Build fixed-width carrier C types from guest LLVM lowering evidence, ask Clang's
  x86_64-pc-windows-msvc frontend for host lowering, preserve MS x64 positional
  register slots and hidden result behavior, and join guest and host locations into
  explicit transfer records for later generated adapters.

notes: >-
  Mechanical ABI evidence only. Do not modify SysTranslator or runtime behavior,
  call host implementations, invent semantic compatibility, or overlap the active
  Darwin pthread runtime claim. The carrier ABI deliberately preserves Apple value
  widths instead of recompiling Apple source spellings under LLP64 Windows.
