status: active
owner: DeerSpotter
agent: ChatGPT GPT-5.6 Sol

task: >-
  Make the generic Windows IPA tester automatically locate a nearby valid iOS
  RuntimeRoot during drag-and-drop testing instead of stopping at Foundation
  solely because the second command-line argument was omitted.

scope:
  - scripts/Test-Ipa.ps1
  - scripts/Test-Ipa.cmd
  - .github/workflows/windows-arm64-core.yml

branch: tester/runtime-root-autodetect
started_utc: 2026-08-30T14:00:00Z
updated_utc: 2026-08-30T14:00:00Z
base_sha: 4e7f7f700287f1c674ac8e47f53df247c940eead
expected_pr: tbd

next_checkpoint: >-
  Validate generic RuntimeRoot detection and rebuild the Windows tester so the
  fixed ZIP is automatically published to tester/windows/latest on master.

notes: >-
  No private IPA name or local absolute path is hardcoded. RuntimeRoot candidates
  must contain top-level System and usr directories before they are accepted.
