status: active
owner: DeerSpotter
agent: ChatGPT GPT-5.6 Sol

task: >-
  Prove the complete pinned GitHub-hosted iOS 18.5 RuntimeRoot can be packaged as
  one DwarFS image and consumed directly by ipaSim on real Windows without
  mounting or extracting the RuntimeRoot tree, then prove the image-backed
  loader reaches the same first real compatibility boundary as the directory
  RuntimeRoot backend on the same exact head.

scope:
  - README.md
  - include/ipasim/IpaSimulator.hpp
  - include/ipasim/Probe.hpp
  - include/ipasim/DynamicLoader.hpp
  - src/IpaSimulator/IpaProbe.cpp
  - src/IpaSimulator/IpaSimulator.cpp
  - src/IpaSimulator/DynamicLoader.cpp
  - src/IpaSimulator/CMakeLists.txt
  - .github/workflows/runtime-root-dwarfs-reader.yml
  - tools/ci/build-dwarfs-fixture.sh
  - tools/ci/build-dwarfs-reader.ps1
  - tools/ci/build-runtime-dwarfs.sh
  - tools/ci/resolve-core-tester.sh
  - tools/ci/run-dwarfs-acceptance.ps1
  - tools/ci/dwarfs-diagnostic.js
  - .github/agent-work/runtime-root-dwarfs-acceptance.yml

branch: runtime-root-dwarfs-acceptance
started_utc: 2026-09-03T21:47:00Z
updated_utc: 2026-09-03T23:45:00Z
base_sha: 9fb67da54cb7c13cdea2a68558d9a734215174bb
expected_pr: draft

next_checkpoint: >-
  Record the successful full-image Windows loader result in README, then run the
  pinned public AWS IPA through both DwarFS and extracted-directory RuntimeRoot
  backends using the same exact-head Core tester. If both stop at the same first
  genuine non-cascading compatibility boundary, mark PR #78 ready and merge it.

notes: >-
  The full DwarFS path now reaches real RuntimeRoot Mach-O loading on Windows and
  currently stops at unresolved _mach_absolute_time while applying chained fixups
  for libsystem_sim_platform.dylib. Keep the existing trusted directory/zstd
  RuntimeRoot path unchanged until exact-head parity is proven. No exclusions,
  path rewriting, mounts, extraction fallback, hidden failures, or suppressed
  diagnostics.