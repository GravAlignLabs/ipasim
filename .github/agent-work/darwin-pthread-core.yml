status: active
owner: DeerSpotter
agent: ChatGPT GPT-5.6 Sol

task: >-
  Implement the Darwin pthread core surface used by libdispatch and dyld as one
  runtime subsystem: exact LP64 pthread_attr semantics, thread identity, QoS and
  parallelism queries, and real pthread create/exit/detach lifecycle backed by
  independent ARM64 Unicorn contexts on Windows threads.

scope:
  - src/IpaSimulator/DarwinPthreadCoreBridge.cpp
  - src/IpaSimulator/DarwinPthreadCoreSmoke.cpp
  - src/IpaSimulator/DarwinPthreadTsdBridge.cpp
  - src/IpaSimulator/DarwinPthreadWorkqueueBridge.cpp
  - src/IpaSimulator/IpaSimulator.cpp
  - src/IpaSimulator/SysTranslator.cpp
  - include/ipasim/IpaSimulator.hpp
  - include/ipasim/Probe.hpp
  - include/ipasim/SysTranslator.hpp
  - src/IpaSimulator/CMakeLists.txt
  - .github/agent-work/darwin-pthread-core.yml

branch: compat-darwin-pthread-core-v2
started_utc: 2026-08-31T04:48:00Z
updated_utc: 2026-09-02T22:05:00Z
base_sha: fa2cc3475886725dcb6b06fdabf11bdc32c785e2
expected_pr: draft

next_checkpoint: >-
  Rebase the narrow pthread-core implementation onto current master, expose real
  guest execution-stack bounds for pthread_get_stackaddr_np, add only the core
  pthread host-call signatures required for guest routing, preserve exact
  64-byte Darwin pthread_attr behavior and independent ARM64 worker execution,
  then run the complete existing semantic smoke graph without regression.

notes: >-
  The historical compat-darwin-pthread-core branch is retained as implementation
  evidence but is 379 commits behind current master and must not be merged
  directly. SysTranslator.cpp/.hpp are reacquired narrowly because current
  master has no host-call signatures for the new core pthread exports and the
  execution-context stack is private to SysTranslator; both are required for a
  truthful guest-call path. Do not implement pthread_create as a synchronous
  main-engine callback. Preserve truthful error/lifetime behavior, do not
  fabricate stack or scheduler state, and keep the current generated routing and
  descriptor work intact.
