status: active
owner: DeerSpotter
agent: ChatGPT GPT-5.6 Sol

task: >-
  Implement the Darwin pthread core surface used by libdispatch and dyld as one
  runtime subsystem: exact LP64 pthread_attr semantics, thread identity, QoS and
  parallelism queries, and real pthread create/exit/detach lifecycle backed by
  independent ARM64 Unicorn contexts on Windows threads.

scope:
  - src/IpaSimulator/DarwinPthreadCoreBridge.cpp
  - src/IpaSimulator/DarwinPthreadCoreSmoke.cpp
  - src/IpaSimulator/DarwinPthreadTsdBridge.cpp
  - src/IpaSimulator/DarwinPthreadWorkqueueBridge.cpp
  - src/IpaSimulator/IpaSimulator.cpp
  - include/ipasim/IpaSimulator.hpp
  - include/ipasim/Probe.hpp
  - src/IpaSimulator/CMakeLists.txt
  - .github/agent-work/darwin-pthread-core.yml

branch: compat-darwin-pthread-core
started_utc: 2026-08-31T04:48:00Z
base_sha: 960ca890cf93a4564e346bc02ad942ba9933623c
expected_pr: tbd

next_checkpoint: >-
  Rebase the existing pthread-core implementation onto current master, preserve
  exact 64-byte Darwin pthread_attr behavior, guest pthread identity/lifetime
  state, independent ARM64 worker execution, destructor/exit cleanup, and the
  libdispatch pthread import neighborhood with executable smoke tests.

notes: >-
  SysTranslator.cpp is deliberately released from this claim as of 2026-09-02
  so generated semantic-routing migration can continue without overlap. The
  current pthread branch has no SysTranslator diff. If later callback-affinity
  work genuinely requires SysTranslator, rebase first and acquire a fresh narrow
  claim rather than relying on this historical reservation. Do not implement
  pthread_create as a synchronous main-engine callback. Preserve truthful
  error/lifetime behavior.
