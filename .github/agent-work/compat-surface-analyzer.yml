status: active
owner: DeerSpotter
agent: ChatGPT GPT-5.6 Sol

task: >-
  Add a target-neutral offline compatibility-surface analyzer that reads ARM64
  Mach-O dependencies and chained imports, emits a deterministic machine-readable
  manifest, and validates the analyzer with synthetic fixtures and self-reporting CI.

scope:
  - tools/compat_surface/compat_surface.py
  - tools/compat_surface/test_compat_surface.py
  - tools/compat_surface/README.md
  - .github/workflows/compat-surface.yml
  - .github/agent-work/compat-surface-analyzer.yml

branch: compat-surface-analyzer
started_utc: 2026-08-31T13:05:21Z
updated_utc: 2026-08-31T13:05:21Z
base_sha: 8de8fe4428ed0a145d1fdd6970583a7dbdd4480f
expected_pr: draft

next_checkpoint: >-
  Implement deterministic thin/fat ARM64 Mach-O dependency and chained-import
  parsing, emit JSON grouped by importer/provider, prove it with synthetic tests,
  and wire a persistent PR diagnostic that publishes the first actionable failure
  before failing CI normally.

notes: >-
  This increment is diagnostic and metadata-only with respect to emulator runtime
  behavior. It must not patch imports, fabricate compatibility, or overlap the
  active pthread/stat runtime claims.
