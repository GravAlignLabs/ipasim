status: active
owner: DeerSpotter
agent: ChatGPT-GPT-5.6-Sol

task: >-
  Implement Darwin mach_continuous_time and mach_timebase_info semantics on the
  Windows host bridge with coherent ARM64 guest ABI translation and focused
  semantic smoke coverage.

scope:
  - .github/workflows/windows-arm64-core.yml
  - src/IpaSimulator/CMakeLists.txt
  - src/IpaSimulator/DarwinKernelBridge.cpp
  - src/IpaSimulator/DarwinKernelSmoke.cpp
  - src/IpaSimulator/DarwinTimeBridge.cpp
  - src/IpaSimulator/DarwinTimeSmoke.cpp
  - src/IpaSimulator/SysTranslator.cpp

branch: feature/darwin-continuous-time
started_utc: 2026-08-29T19:36:00Z
updated_utc: 2026-08-29T20:58:00Z
base_sha: 22d7b343467f322e0dd6e1358717f75421504b42
expected_pr: draft #4

next_checkpoint: >-
  Validate the continuous-time boundary through public CI and keep the kernel
  semantic smoke honest by reading guest-visible errno through the Darwin
  __error export instead of the smoke executable's separate CRT state.

notes: >-
  Scope includes the narrow CMake timing link, PR diagnostic workflow, and
  DarwinKernelSmoke correction exposed by CI. The host csops boundary already
  reports unsupported semantics and sets its own thread-local errno; the smoke
  must inspect that state through the exported Darwin __error ABI. Public,
  target-neutral compatibility work only; no private application data is included.
