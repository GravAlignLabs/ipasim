// Explicit semantic approval data for loader-selected generated imports.
//
// This file is intentionally NOT generated from ABI metadata or a static symbol
// inventory. A row exists only after the provider semantics are independently
// approved. Provider addresses remain runtime facts and are verified against the
// real PE export before a route is selected.
static constexpr ApprovedSemanticImportRoute ApprovedSemanticImportRoutes[] = {
    {
        "_getpid",
        "getpid",
        L"ipasimdarwinhost.dll",
        "_getpid",
        "DarwinHostBridge.getpid",
        LiveGuestProfile::NoArgumentsSInt32ToX0,
    },
};
