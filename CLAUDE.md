# Claude Code Instructions

@AGENTS.md

`AGENTS.md` is the canonical project instruction file for ipaSim. Follow it for all work in this repository.

When changing emulator-core files under `src/IpaSimulator/`, also follow:

@src/IpaSimulator/AGENTS.md

Do not replace these instructions with tool-specific behavior. In particular, preserve the repository rules around target-neutral public reproduction, explicit unsupported behavior, no fake-success compatibility, no monkey patching, and the documented CI validation/diagnostic flow.
